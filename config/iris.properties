#
# Stores the iris configuration, by default "shaderPack" is set to null for disabled as shaders are FPS-Consuming,
# For maxed shadow rendering, 5 will be the default instead.
#

maxShadowRenderDistance=5
shaderPack=null
enableShaders=false
